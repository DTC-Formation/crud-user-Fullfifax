{% extends 'base.html.twig' %}

{% block title %}Calcul result{% endblock %}

{% block body %}
    <h2 class="text-orange text-center text-2xl font-bold my-4">Result: {{ total }}</h2>
    <div class="flex justify-center">
        <a class="mt-6 text-white underline text-white px-4 py-2" href="{{ path('app_homepage') }}">Back to homepage</a>
    </div>
{% endblock %}